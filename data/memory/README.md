# Phase-2B Memory Runtime Data

This directory is reserved for Phase-2B Memory runtime data.

Current intended SQLite path:

```text
data/memory/uaea_memory.db
```

Database files are local runtime artifacts and are ignored by Git. The tracked
README keeps the directory boundary visible without committing SQLite runtime
state.

Current status:

```text
SQLite environment smoke only.
No Memory schema.
No Memory CRUD API.
No Runtime integration.
```
