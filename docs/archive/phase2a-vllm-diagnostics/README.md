# Phase-2A vLLM Diagnostic Document Archive

This directory stores historical diagnostic notes produced while isolating the
DS14B AWQ vLLM failure. These documents are no longer the active Phase-2A
status surface, but they are retained to preserve the evidence chain.

Current active summary remains:

```text
docs/phase2a-vllm-root-cause-analysis.md
```

The active production candidate is:

```text
Qwen2.5-14B-Instruct-AWQ
```

Archived documents should be treated as historical context, not current
operational guidance.

